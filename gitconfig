[core]
    editor = vim
    pager = diff-highlight | less -r
[credential]
    helper = cache --timeout=3600
[diff]
    algorithm = histogram
[alias]
    st = status 
    co = checkout
    ci = commit
    br = branch
    du = diff -b
    ds = diff -b --staged
[include]
    path = ~/.gitconfig.local
